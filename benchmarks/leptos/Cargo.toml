[package]
name = "bench-leptos"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
leptos = { version = "0.8", features = ["csr"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
