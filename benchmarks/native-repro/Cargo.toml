[package]
name = "native-repro"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
glory-core = { path = "../../crates/core", default-features = false, features = ["web-ssr"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
